

<div class="col-md-8 col-centered">
  <div class="panel panel-primary">
    <div class="panel-body">
      <h2> <%= @job.title %></h2>
    </div>
    <div class="panel-footer">
      <p>
        <%= simple_format(@job.description) %>
      </p>
      <div class="text-left">
        <br/>
        <%= link_to("申请职位", new_job_resume_path(@job),  class:"btn btn-danger btn-lg") %>
      </div>
    </div>
  </div>
</div>
