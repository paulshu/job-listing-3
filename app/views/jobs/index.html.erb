<!-- <div class="pull-right">
  <%= link_to("添加工作", new_job_path, :class => "btn btn-sm btn-info" ) %> <!-- 如设置new_admin_job_path路径则会检查用户是否为管理员
</div>

<% if current_user %>
<div class="row">
  <div class="col-md-2">
    <ul class="nav nav-pills nav-strcked" style="max-width: 300px; padding-top:20px;" >
      <li class="active"><%= link_to("返回管理页面", admin_jobs_path )%> </li>
    </ul>
  </div>
</div>
<% end %>

<div class="dropdown clearfix pull-right">
  <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true" >
    排序
      <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
    <li>
      <%= link_to("按薪资下限排序", jobs_path(:order => "by_lower_bound")) %>
    </li>
    <li>
      <%= link_to("按薪资上限排序", jobs_path(:order => "by_upper_bound")) %>
    </li>
    <li>
      <%= link_to("按照发布时间排序", jobs_path) %>
    </li>
  </ul>
</div>

<table class="table table-boldered">
  <thead>
    <tr>
      <td>职缺</td>
      <td>薪资下限</td>
      <td>薪资上限</td>
      <td>发布时间</td>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
    <tr>
      <td>
        <%= link_to(job.title, job_path(job)) %>
      </td>
      <td>
        <%= job.wage_lower_bound %>
      </td>
      <td>
        <%= job.wage_upper_bound %>
      </td>
      <td>
        <%= job.created_at %>
      </td>
    </tr>
    <% end %>
</tbody>
</table> -->


<div class="col-md-10 col-centered">

<div class="panel panel-info">
  <div class="panel-heading">
    <h3>求职列表
      <div class="dropdown clearfix pull-right">
        <button class="btn btn-success dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true" >
          排 序
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
          <li>
            <%= link_to("按薪资下限排序", jobs_path(:order => "by_lower_bound")) %>
          </li>
          <li>
            <%= link_to("按薪资上限排序", jobs_path(:order => "by_upper_bound")) %>
          </li>
          <li>
            <%= link_to("按照发布时间排序", jobs_path) %>
          </li>
        </ul>
      </div>
      <div class="pull-right">
        <%= link_to("添加工作", new_job_path, :class => "btn btn-info" ) %> &nbsp  <!-- 如设置new_admin_job_path路径则会检查用户是否为管理员  &nbsp插入一个空格-->
      </div>
    </h3>

  </div>
  <div class="panel-footer">
    <table class="table table-boldered">
      <thead>
        <tr>
          <td>职缺</td>
          <td>薪资下限</td>
          <td>薪资上限</td>
          <td>发布时间</td>
        </tr>
      </thead>

      <tbody>
        <!-- <% @jobs.each do |job| %>
        <tr>
          <td>
            <%= link_to(job.title, job_path(job)) %>
          </td>
          <td>
            <%= job.wage_lower_bound %>
          </td>
          <td>
            <%= job.wage_upper_bound %>
          </td>
          <td>
            <%= job.created_at %>
          </td>
          <td>
            <% if current_user && current_user.is_favorite_of?(job) %>
              <%= link_to favorite_job_path(job), method: :post do %><i class="fa heart fa-lg" aria-hidden="true"></i><% end %>
            <% else %>
              <%= link_to unfavorite_job_path(job), method: :post do %><i class="fa fa-heart-o fa-lg" aria-hidden="true"></i><% end %>
            <% end %>
          </td>
        </tr>
        <% end %> -->
        <%= render :partial => "job_item", :collection =>@jobs, :as => :job %>
      </tbody>
    </table>
  </div>
</div>
</div>
